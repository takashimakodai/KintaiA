<div class="modal-dialog modal-lg modal-dialog-center">
  <div class="modal-content">
    <div class="modal-header">
      <button type="button" class="close" data-dismiss="modal" aria-label="Close">
        <span aria-hidden="true">&times;</span>
      </button>
    </div>
  　<h1 class="modal-title">勤怠ログ</h1>
    <div class="modal-body">
      <div class="row">
        <div class="col-md-12">
          <table class="table table-bordered table-striped table-condensed overwork-table">
            <thead class="overwork-table">
              <%= form_with url: attendances_log_info_user_path, method: :get, local: true do |f| %>
                <tr>
                  <th><%= f.submit "検索年月選択",class: "btn btn-sm btn-primary" %></th>
                  <th><%= f.date_select :worked_on, { include_blank: true, 
                                                      discard_day: true,
                                                      date_separator: "-",
                                                      use_month_numbers: true,
                                                      start_year: Date.today.year,
                                                      end_year: @attendances.first.try(:worked_on).try(:year)} %>
                  </th>
                </tr>
                <tr>
                  <th>日付</th>
                  <th>変更前出社時間</th>
                  <th>変更前退社時間</th>
                  <th>変更後出社時間</th>
                  <th>変更後退社時間</th>
                  <th>指示者</th>
                  <th>承認日</th>
                </tr>
              </thead>
              <% @attendances.each do |day| %>
                <tbody>
                  <tr>
                    <td><%= l(day.worked_on, format: :defaults) %></td>
                    <td><%#= l(day.change_started_at.floor_to(15.minutes), format: :time) %></td>
                    <td><%#= l(day.change_finished_at.floor_to(15.minutes), format: :time) %></td>
                    <td><%#= l(day.started_at.floor_to(15.minutes), format: :time) %></td>
                    <td><%= l(day.started_at, format: :time) if day.started_at.present? %></td>
                    <td><%= day.confirmation_mark %></td>
                    <td><%= l(day.updated_at, format: :longs) %></td>
                  </tr>
                </tbody>
              <% end %>
            <% end %>
          </table>
        </div>
      </div>
    </div>
  </div>
</div>

















